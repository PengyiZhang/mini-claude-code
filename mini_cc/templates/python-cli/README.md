# {{PROJECT_DISPLAY_NAME}}

A Python CLI project. Replace this README with your own.

## Layout

- `src/` — package source
- `tests/` — pytest suite
- `pyproject.toml` — project metadata + tool config

## Develop

```bash
pip install -e .
pytest
```
